<%= form_with(model: profile_land) do |form| %>
  <% if profile_land.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(profile_land.errors.count, "error") %> prohibited this profile_land from being saved:</h2>

      <ul>
        <% profile_land.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :profile_id, style: "display: block" %>
    <%= form.text_field :profile_id %>
  </div>

  <div>
    <%= form.label :land_id, style: "display: block" %>
    <%= form.text_field :land_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
